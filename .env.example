# Optional. If set, the "Ask WayZero" advisory bot (app/api/advisory/ask)
# will call the real Anthropic API for its answers instead of the built-in
# rule-based matcher. Leave unset and everything still works — the bot just
# falls back to rule-based matching against known route names/keywords.
# ANTHROPIC_API_KEY=sk-ant-...
